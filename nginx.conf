events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location / {
            # Direciona todo o tráfego para a nossa API em Go
            proxy_pass http://api:8080;
        }
    }
}